apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix
  namespace: observability
spec:
  stripPrefix:
    prefixes:
      # - /grafana
      - /prometheus
    forceSlash: true